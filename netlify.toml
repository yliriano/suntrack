[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirect all routes to index.html so React Router / client-side routing works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
